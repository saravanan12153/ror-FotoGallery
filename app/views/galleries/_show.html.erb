<% @images.each do |image| %>
    <%= link_to gallery_image_path(:gallery_id => @gallery.id, :id => image ) do %>
        <div class="img-thumbnail"><%= image_tag image.avatar.url(:thumb), alt: image.avatar.url(:thumb) %></div>
    <% end %>
<% end %>